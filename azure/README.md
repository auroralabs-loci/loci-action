## LOCI for Azure DevOps

This is a placeholder. Full user-facing documentation lands in a later commit.

The Azure DevOps extension provides a `LociTask@1` pipeline task that uploads
compiled binaries to the LOCI backend for performance analysis. It is the
Azure analogue of the `auroralabs-loci/loci-action@v1` GitHub Action.
